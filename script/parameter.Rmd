```{r}
#Choose group to import either control or gbm
radiation <- c("control","radiated")
runs <-  c("run1","run2")
```

```{r}
# for cell type annotation sheet
sig.subtype.sheets <- c(1,2,3)
names(sig.subtype.sheets) <- c("6_metamodule","4combined","4equal")
subsheet <- sig.subtype.sheets[1]

```

```{r}
object <- "all"
```

```{r}
#Common config
##Metamodule choice
###This is for 6 metamodule


sigs <- c("6metamodules","4metamodules_new","4metamodules_new_50","MESImm")
sig <- sigs[3]
sig
```
```{r}
#For reference based
merges <- c("6metamodules","4_merge_metamodules")
merge <- merges[2]
celltypes <- c("celltype","celltype_merge")
celltype <- celltypes[2]
```

#annotation
```{r}
#For tool to be analized in annotation
anno.picks <- c(by_type=1, by_consistency = 2, only_diverge = 3, marker_based = 4)
anno.pick <- anno.picks[3]
```

